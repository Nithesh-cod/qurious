node_modules/
dist/
android/
*.apk

# Secrets. Never commit these — this repository is a public SIH deliverable.
.env
.env.*
!.env.example
# Holds no secret — it exists precisely to blank the key out of shipped builds.
!.env.production

server/quantum_learning.db
server/__pycache__/
.DS_Store

# Local build cache
tsconfig.tsbuildinfo
